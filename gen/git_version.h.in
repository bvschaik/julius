// DO NOT EDIT. This file (and "GIT_COMMIT_HASH") is generated by CMake.
// Run CMake configure step to update it.
#ifndef _GIT_VERSION_H_
#define _GIT_VERSION_H_

#include <string.h>
#include <stdio.h>

#define STRINGIFY(x) #x
#define TOSTRING(x) STRINGIFY(x)

#define GIT_COMMIT_HASH @GIT_COMMIT_HASH@

const char* git_version_information(void)
{
    char version[100];
    
    snprintf(version, 100, "%s", TOSTRING(GIT_COMMIT_HASH));
    
    // check if string only contains nothing. This means no version information provieded within define GIT_COMMIT_HASH
    if(strlen(version) == 0)
    {
        return (const char*)"unknown";
    }
    return (const char*)TOSTRING(GIT_COMMIT_HASH);
}

#endif // _GIT_VERSION_H_